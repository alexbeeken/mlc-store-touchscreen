{{#title-bar home=true}}
  Tufa as a Benchmark
{{/title-bar}}
{{photo-slide slide=currentSlide}}
<div class='photo-links'>
  {{#each slides as |slide index|}}
    <div class='photo-link'>
      <a {{action 'switchIdx' index}}>
        <img src={{slide.path}}>
      </a>
    </div>
  {{/each}}
</div>
